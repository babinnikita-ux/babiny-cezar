# Handoff — R4
## State
Run started; next Step 1.1. Same branch/PR #396. Composer component is shared from R3 (`components/composer/`). Legacy pin of /new lives in `src/server/static-ui.ts` (resolveGetRequest).
