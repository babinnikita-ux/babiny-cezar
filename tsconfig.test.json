{
  // Typecheck-only view of the server: the build config plus the test files it excludes,
  // plus the vitest workspace root config. Never emits.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "test/**/*.ts", "vitest.config.ts"],
  "exclude": []
}
