// Placeholder shell. The real sidebar/router shell lands in Step 2.3; design tokens land in Step 1.2,
// so this stays deliberately unstyled — no colors, no classes.
export function App() {
  return (
    <main>
      <h1>cezar</h1>
      <p>The redesigned cockpit is being assembled. The current UI is available at ?legacy=1.</p>
    </main>
  )
}
